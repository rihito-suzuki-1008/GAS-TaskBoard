# マイルストーン・会議体 技術仕様

最終更新: 2026-07-03
参照元: `docs/01要件定義・設計/要件定義書.md` / `docs/01要件定義・設計/基本設計書.md` / `DESIGN.md` / `AGENTS.md`

## 関連ファイル

- `src/10_WbsExport.js`: `upsertMilestone()`, `deleteMilestone()`, `upsertMeeting()`, `deleteMeeting()`。
- `src/ClientRenderPanels.html`: `renderProjectSettingsDialog()`。
- `src/ClientActions.html`: プロジェクト設定のdraft管理と保存/削除。

## データ

- `Milestones`: MilestoneId, Name, Date, Note, SortOrder。
- `Meetings`: MeetingId, Name, Schedule, Note, SortOrder。
- どちらも `HEADERS` に含め、通常の `ensureSchema_()` 対象です。
- WBSシートとは異なり、アプリの正本データシートです。

## 保存

- APIは `withLock_()` と `requireCurrentMember_()` を使います。
- UpdatedAt照合は行いません。
- クライアント採番IDを受け取り、重複IDは拒否します。
- クライアントは `queueBackgroundMutation('milestones'...)` / `queueBackgroundMutation('meetings'...)` で楽観的更新します。
